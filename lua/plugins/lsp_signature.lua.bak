local P = {
  'ray-x/lsp_signature.nvim',
  event = 'InsertEnter',
  opts = {
    bind = true,
    max_height = 4,
    max_width = 66,
    floating_window = false,
    hint_enable = true,
    hint_scheme = 'lspSignatureHint',
    padding = ' ',
    -- toggle_key = '<C-q>',
  },
}

return P
